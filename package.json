{
  "name": "neuropetrix-monorepo",
  "version": "3.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "install": "cd apps/web && npm install"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
